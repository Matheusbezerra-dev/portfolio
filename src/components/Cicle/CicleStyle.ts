import styled from "styled-components";

export const ContainerCicle = styled.div`

`;

export const Canvas = styled.canvas`
  width: 31,25;
  height: 31,25;
`;

export const ContainerUL = styled.canvas`
`;